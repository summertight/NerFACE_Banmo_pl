gpu: [0]
working_dir : None
resume_path: None
method: nerface

dataset:
  name: 'voxceleb'
  basedir: '/home/nas1_userB/sunghyun/Project/Sparse-Nerface/nerface_dataset/person_1'
  H: 256
  W: 256
  no_ndc: True #  Do not use NDC (normalized device coordinates). Usually True for synthetic (Blender) datasets.
  near: 0.2
  far: 0.8
  batch_size: 1
  val_batch_size: 1
  data_size: 194 #XXX Change this elaborately! #잘된애로 다시!
  num_workers: 4

model:
  num_coarse: 64 # Number of depth samples per ray
  num_fine: 64 # Number of depth samples per ray
  lindisp: False
  perturb: True
  
train_params:
  num_iters: 1000000
  lr: 0.0005
  checkpoint_freq: 100000
  num_random_rays: 1024 #64 * 2048 장당 데이터 갯수
  chunksize: 1024
  radiance_field_noise_std: 0.1
  trans_set_0: False

val_params:
  chunksize: 65536
  radiance_field_noise_std: 0.0

# TODO: change it to enable CLI
hydra:
  job:
    #img_RT순서
    id: vox_IM_IM_vox_segmask_as_GT_and_sampling_map_accurate_datasize_previousgood
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}_${hydra.job.id}